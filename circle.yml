dependencies:
  cache_directories:
    - ".tox"

  override:
    - pip install -r requirements-dev.txt
    - pyenv local 2.7.10

test:
  override:
    - make test
    - make docs

deployment:
  master:
    branch: master
    commands:
      - aws s3 sync docs/_build/html s3://juicedocs/projects/juicecli --acl private --delete
